using System.Collections; using System.Collections.Generic; using UnityEngine;  public class GazeHandler : HoloToolkit.Unity.Singleton<GazeHandler> {     private Color startColor;      // Use this for initialization     void Start()     {     }      // Update is called once per frame     void Update()     {     }      void OnGazeEnter()     {         Debug.Log("Test 1");         var com = gameObject.GetComponent<Renderer>();         startColor = com.material.color;         com.material.color = Color.yellow;     }      void OnGazeExit()     {         Debug.Log("Test 2");         var com = gameObject.GetComponent<Renderer>();         com.material.color = Color.red;     } }